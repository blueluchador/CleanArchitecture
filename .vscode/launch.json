{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug in Docker",
      "type": "docker",
      "request": "attach",
      "platform": "netCore",
      "sourceFileMap": {
        "/src": "${workspaceFolder}"
      },
      "containerName": "run-local-clean-architecture.api-1"
    }
  ]
}